# Longhorn prod overlay — ingress for the Longhorn UI dashboard.
# Replace longhorn.<domain> with your actual domain.
ingress:
  enabled: true
  ingressClassName: nginx
  host: longhorn.fhassis.top
  tls: false
