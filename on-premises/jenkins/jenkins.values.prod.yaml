# Jenkins prod overlay — real domain, TLS, production resource limits.

controller:
  jenkinsUrl: "https://jenkins.fhassis.top"

  resources:
    requests:
      cpu: "500m"
      memory: "1Gi"
    limits:
      cpu: "2000m"
      memory: "4Gi"

  ingress:
    enabled: true
    ingressClassName: nginx
    hostName: jenkins.fhassis.top
    annotations:
      nginx.org/ssl-redirect: "true"
      nginx.org/location-snippets: |
        proxy_set_header Accept-Encoding "";
        sub_filter '</head>' '<script async defer src="https://umami.fhassis.top/script.js" data-website-id="REPLACE_WITH_UUID"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    tls:
      - hosts:
          - jenkins.fhassis.top
        # secretName intentionally absent — nginx uses default-ssl-certificate (kube-system/ssl-certificate)
